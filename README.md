# airline_reservation_system
This simple airline booking system project file contains a python script (pyt.py) and a database file (SQLite). This is a simple GUI based application which is very easy to understand and use. It uses Tkinter module for the GUI. Talking about the application, the user can simply book a flight, cancel it and search for flights.
